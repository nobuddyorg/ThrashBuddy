spring.application.name=thrashbuddy
# Trust X-Forwarded-* headers from the ingress so the CORS same-origin check
# (and any absolute URLs Spring builds) see the client-facing host/port/scheme,
# not the internal proxy hop.
server.forward-headers-strategy=framework
